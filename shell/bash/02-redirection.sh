#!/bin/bash

# cat >> to a file
# echo "line" >> to a file
# echo "overwrite whole file" > file

# cat << mlc_start "whatever typed here to terminal
#		   "or can be from a file" mlcstart
# cat file (to stdout)
